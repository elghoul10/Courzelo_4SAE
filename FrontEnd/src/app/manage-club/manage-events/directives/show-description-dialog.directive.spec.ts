import { ShowDescriptionDialogDirective } from './show-description-dialog.directive';

describe('ShowDescriptionDialogDirective', () => {
  it('should create an instance', () => {
    // const directive = new ShowDescriptionDialogDirective();
    // expect(directive).toBeTruthy();
  });
});
